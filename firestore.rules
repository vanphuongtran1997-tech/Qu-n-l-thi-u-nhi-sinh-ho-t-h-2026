rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows public read and write access to all documents.
    // WARNING: This is extremely insecure and should only be used for prototyping.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
